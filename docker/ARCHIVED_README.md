# Archived Docker assets

This folder contains the previous Docker build files and scripts for Agent Zero. Docker support has been archived in this branch to prioritize local-first usage.

To restore Docker support, move the contents back to `docker/` and update any paths that reference `/a0` or other container-only locations.
